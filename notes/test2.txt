package main

import "fmt"

func main() {
	a := []int{1, 2, 2, 3, 1, 4, 2}
	m := make(map[int]int)

	for _, v := range a {
		if m[v] == 0 {
			m[v] = 1
		} else {
			m[v]++
		}

	}

	fmt.Printf("Map : %+v\n", m)

}
